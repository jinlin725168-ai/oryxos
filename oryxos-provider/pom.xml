<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.oryxos</groupId>
    <artifactId>oryxos-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>oryxos-provider</artifactId>
  <name>oryxos-provider</name>
  <description>核心能力一：ProviderService、Function Calling 适配、provider-name 到 ChatModel 显式映射</description>

  <dependencies>
    <dependency>
      <groupId>com.oryxos</groupId>
      <artifactId>oryxos-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter</artifactId>
    </dependency>

    <!-- TODO(US-1)：接入 Spring AI Alibaba（技术方案 §3，宪法原则四）。
         实现 Provider 抽象时激活，并把版本钉到与所用 Spring Boot 兼容的一档
         （当前稳定线 1.1.2.x，子构件版本号需逐个核对；可能需要额外的 Spring 里程碑仓库）。
         注意：只用其 Provider 抽象 + 协议转换 + @Tool schema 生成，禁用其自动 tool 执行。

    <dependency>
      <groupId>com.alibaba.cloud.ai</groupId>
      <artifactId>spring-ai-alibaba-starter-dashscope</artifactId>
      <version>1.1.2.1</version>
    </dependency>
    -->
  </dependencies>
</project>
